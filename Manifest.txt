

    Debug.py
    The main debug program to import as a library - we can't call it DebugLibrary because there is already one of that name
    
    DebugUI.py    
    Debugger User Interface to try commands for robotFramework scripts

    RfInterface.py
    Interface to allow Debug to discover xpaths and RF variables

    defaultVars.py
    A list of the default variables in robotFramework - so we can ignore them when listing variables
